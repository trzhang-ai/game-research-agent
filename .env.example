# Required for OpenAI chat completions and embeddings.
OPENAI_API_KEY=replace_me

# Required only for the web-fallback path.
TAVILY_API_KEY=replace_me

# Override only when using an OpenAI-compatible endpoint.
OPENAI_BASE_URL=https://api.openai.com/v1
